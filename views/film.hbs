<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Страница фильма</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="index.css">
</head>

<body>

    <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
                <span class="fs-4">Фильмопоиск 2.0</span>
            </a>
        </header>
    </div>

    <main>
        <div class="container">
            <div class="row row-cols-1 row-cols-lg-2 g-5">
                <div class="col">
                    <article>
                        <h2 class="mb-1">{{film.title}}</h2>
                        <p>{{film.year}}</p>
                        {{#each film.actors}}
                        <a href="/actor?id={{this._id}}">{{this.fullName}}</a>
                        {{/each}}
                        <hr>
                    </article>
                    <form action="/add-comment?film_id={{film._id}}" method="POST" class="new-comment">
                        <div class="input-group mb-3">
                            <span class="input-group-text">Имя</span>
                            <input name="author" type="text" class="form-control">
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text">Оценка</span>
                            <input name="rating" type="number" class="form-control">
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text">Комментарий</span>
                            <textarea name="text" type="text" class="form-control"></textarea>
                        </div>


                        <button type="submit" class="btn btn-primary">Отправить</button>
                    </form>
                </div>
                <div class="col">
                    <section>
                        <h2>Комментарии</h2>

                        {{!-- Выведи комментарии --}}
                        {{#each comments}}
                        <div class="card my-3">
                            <div class="card-body">
                                <div>
                                    {{this.author}} - оценка {{this.rating}}
                                </div>
                                <div>
                                    {{this.text}}
                                </div>
                                <div>
                                    <a href="/remove-comment?comment_id={{this._id}}&index={{@index}}">
                                        Удалить
                                    </a>
                                </div>
                            </div>
                        </div>
                        {{/each}}

                    </section>
                </div>
            </div>
        </div>
    </main>
</body>

</html>